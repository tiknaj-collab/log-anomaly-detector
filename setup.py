from setuptools import setup, find_packages

setup(
    name="log-anomaly-detector",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "pandas",  # add any other dependencies your project needs
    ],
)